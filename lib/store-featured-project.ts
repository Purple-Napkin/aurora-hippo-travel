/** SSR-passed featured bundle for the travel home hero (right column). */
export type StoreFeaturedProject = {
  slug: string;
  title: string;
  description: string | null;
  imageUrl: string | null;
};
